# Death Bot Web Platform — Angular frontend

Placeholder. The Angular app is scaffolded here in **Phase 3** of the
implementation plan (see `../deathbot-master-prd.md`, steps 19–24).

This directory is the **base directory** for the separate Coolify
`deathbot-web` application, served at `deathbot.captionato.tech`. It talks to
the bot's public API (`api.deathbot.captionato.tech`) over HTTPS/WSS and never
touches Postgres or the filesystem directly.
